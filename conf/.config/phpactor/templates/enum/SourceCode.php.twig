<?php

declare(strict_types=1);

namespace {{ prototype.namespace }};

{% for class in prototype.classes %}
enum {{ class.name }}: string
{
    case EXAMPLE = 'example';
    case ANOTHER = 'another';
}
{% endfor %}
